import React from "react";

const NotFound = () => (
  <div style={{ textAlign: "center", marginTop: "15%" }}>
    <h3>404 page not found</h3>
    <p>
      We are sorry but the page you are looking for <em>does not</em> exist.
    </p>
  </div>
);

export default NotFound;
